package services.computerPlayingStrategy;

import models.Board;
import models.Symbol;

public class EasyComputerPlayingStrategy implements ComputerPlayingStrategy{
    @Override
    public boolean play(Board board, Symbol symbol) {
        //algo

        return true;
    }
}
